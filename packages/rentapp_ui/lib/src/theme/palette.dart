import 'package:flutter/material.dart';

class Palette {
  const Palette._();

  static const Color black = Color(0xFF000000);
  static const Color dark = Color(0xFF232323);
  static const Color transparent = Colors.transparent;
  static const Color green = Color(0xFF21B6A8);
  static const Color white = Color(0xFFFFFFFF);
  static const Color gray = Color(0xFFC3C3C3);
  static const Color grayDark = Color(0xFF9B9B9B);
  static const Color grayLight = Color(0xFFD3D3D3);
  static const Color grayExtraDark = Color(0xFF919191);
  static const Color redErrorAndAlert = Color(0xFFFF6865);

  static const colors = [
    black,
    dark,
    transparent,
    green,
    white,
    gray,
    grayDark,
    grayLight,
    grayExtraDark,
  ];
}
